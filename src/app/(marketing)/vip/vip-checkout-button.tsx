/**
 * Le pass VIP n'est plus en vente — Mboka Hub est devenu 100% gratuit pour
 * les fans. Ce composant est conservé pour compat. d'imports mais ne rend
 * plus de bouton de paiement.
 */
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function VipCheckoutButton(_props: { priceLabel?: string }) {
  return null;
}
